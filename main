#lang br/quicklang
(require "parser.rkt" "game_tokenizer.rkt")

(define (read-syntax path port)
  (define parse-tree (parse path (make-tokenizer port path)))
  (strip-bindings
   #`(module basic-mod TADA/expander
       #,parse-tree)))

(module+ reader
  (provide read-syntax))
